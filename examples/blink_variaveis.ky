// Agora da pra usar variaveis mas de um modo bem primitivo
var blink_delay: int = 500
var led_pin: int = 13

setup {
    pin led_pin is output
}

loop {
    set pin led_pin to high
    delay blink_delay

    set pin led_pin to low
    delay blink_delay
}
